<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:int="http://www.springframework.org/schema/integration"
	xmlns:int-file="http://www.springframework.org/schema/integration/file"
	xmlns:int-jpa="http://www.springframework.org/schema/integration/jpa"
	
	xsi:schemaLocation="
		http://www.springframework.org/schema/integration         http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/beans               http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration/file    http://www.springframework.org/schema/integration/file/spring-integration-file.xsd
		http://www.springframework.org/schema/integration/jpa     http://www.springframework.org/schema/integration/jpa/spring-integration-jpa.xsd
		http://www.springframework.org/schema/context             http://www.springframework.org/schema/context/spring-context.xsd
		">

	<context:component-scan base-package="gov.usgs.cida.ncETLsis" />

	<bean id="gribber" class="gov.usgs.cida.data.grib.NetCDFArchiver"/>
	
	<bean id="archive-spec-fetcher" class="gov.usgs.cida.data.grib.jpa.ArchiveSpecManager">
	</bean>

	<int-file:inbound-channel-adapter directory="/tmp/ncETL/motherlode" id="motherlode"
		channel="motherlode-chan" filename-pattern="*.grib"
		prevent-duplicates="false"
	>
		<int:poller  cron="0 0 0 1 * *"></int:poller>
	</int-file:inbound-channel-adapter>
	
	<int:channel id="motherlode-chan" datatype="java.io.File"/>
	
	<int:header-enricher id="year-month-enricher" input-channel="motherlode-chan" output-channel="ym-chan">
		<int:header name="year" expression="$year"/>
		<int:header name="month" expression="$month"/>
		<int:header name="rfc" expression="$rfc"/>
	</int:header-enricher>
	
	<int:channel id="ym-chan"/>
	
	<!--  see http://static.springsource.org/spring-integration/reference/htmlsingle/#overview-endpoints-aggregator -->
	<int:aggregator id="grib-agg" input-channel="ym-chan" output-channel="lof-chan" 
		release-strategy-expression="size() >= 12"
		correlation-strategy-expression="headers.rfc+headers.year+headers.month"
		expire-groups-upon-completion="true"
	/>
	
	<int:channel id="lof-chan" >
		<!-- assert that headers have year, month, rfc -->
		<!-- assert that payload is list of file -->
	</int:channel>
	
	<int:header-enricher id="rfc-spec-enricher" input-channel="lof-chan" output-channel="grib-chan">
	
		<int:header name="archive-spec" expression="@archive-spec-fetcher.fetch(headers.rfc)" >
		</int:header>
	</int:header-enricher>

	<int:channel id="grib-chan"/>
	
	<int:outbound-channel-adapter id="grib-writer" channel="grib-chan"
		ref="gribber" method="processFiles"
	/>

</beans>
